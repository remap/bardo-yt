.env
cache/
dist/
*.pem
*.key
__pycache__/
*.py[cod]
.venv/
.DS_Store
node_modules/
runtime/
logs/
.wrangler/
# Regenerated by `npx wrangler types`; it mirrors wrangler.jsonc plus the
# local .env, so it differs per machine and is never the source of truth.
worker-configuration.d.ts
